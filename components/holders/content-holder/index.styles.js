import {StyleSheet} from 'react-native';
import {BLACK_TRANSPARENT, WHITE, BLACK} from '../../../constants';

const styles = StyleSheet.create({
  container: {
    shadowColor: BLACK,
    shadowOpacity: 0.03,
    shadowRadius: 4,
    shadowOffset: {width: 8, height: 8},
    backgroundColor: WHITE,
  },
});

export default styles;
/**
width: 100,
height: 100,
 */
