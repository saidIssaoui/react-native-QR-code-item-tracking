import {StyleSheet} from 'react-native';
import {APP_DARK_GRAY, APP_RED} from '../../../../constants';

const styles = StyleSheet.create({
  container: {
    position: 'absolute',
    zIndex: 100,
    borderRadius: 26,
    padding: 4,
    left: 24,
    bottom: 24,
    justifyContent: 'center',
    alignItems: 'center',
  },
});

export default styles;
